{
  "name": "doantotnghiep-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend --omit=dev && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && echo '--- BUILD FINISHED. LISTING FRONTEND/DIST: ---' && (ls -la frontend/dist || echo 'DIST NOT FOUND') && npm install --prefix backend --omit=dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
